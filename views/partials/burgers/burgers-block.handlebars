<li>
	{{burger_name}}
	{{#unless devoured}}
	<button class="eat-burger" style="border-radius:10px;"data-id="{{id}}" data-newdevour="{{true}}">
		{{#if devoured}} {{else}}Devour!{{/if}}
	</button>
	{{/unless}}
</li>
